#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../image-size/bin/image-size.js" "$@"
else
  exec node  "$basedir/../../../../image-size/bin/image-size.js" "$@"
fi
# cmd-shim-target=/Users/linyuxin/my-website/node_modules/.pnpm/@docusaurus+mdx-loader@3.10.1_@swc+core@1.15.33_postcss@8.5.14_react-dom@19.2.6_react@19.2.6__react@19.2.6/node_modules/image-size/bin/image-size.js
