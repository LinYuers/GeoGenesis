#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../webpack/bin/webpack.js" "$@"
else
  exec node  "$basedir/../../../../webpack/bin/webpack.js" "$@"
fi
# cmd-shim-target=/Users/linyuxin/my-website/node_modules/.pnpm/@docusaurus+plugin-content-blog@3.10.1_@docusaurus+faster@3.10.1_@docusaurus+types@3.10_a2b1ec87304c2db2e0d358285dc85433/node_modules/webpack/bin/webpack.js
